[Unit]
Description=Iceisback DNS server

[Service]
WorkingDirectory=/opt/iceisback
ExecStart=/bin/bash -c "/usr/bin/env pipenv install && /usr/bin/env pipenv run python main.py --send-mail"

[Install]
WantedBy=multi-user.target
